# NEXUS · 项目进度

> 更新日期:2026-09-26 · 当前版本:v2.0(阶段四完成)
> 定位:网络安全态势感知平台 —— 从前端演示演进为带真实数据管道的完整系统

## 一、总体进度:四个阶段全部交付

| 阶段 | 主题 | 状态 | 核心交付 |
| --- | --- | --- | --- |
| 阶段〇 | 产品与视觉 | ✅ 完成 | 8 个页面(落地页/认证/控制台/文档/状态页/404)、Editorial Noir 设计系统、开场动画、日食主视觉、PWA |
| 阶段一 | 数据是真的 | ✅ 完成 | 采集器 Agent(tail 真实日志 / 压力生成器 / Syslog UDP 监听)、Ingest API(Bearer Token)、Sigma 风格检测引擎、告警溯源 |
| 阶段二 | 企业化基座 | ✅ 完成 | RBAC 三级角色、审计日志、CSRF 双提交、20 项集成测试、GitHub Actions CI、Docker 部署 |
| 阶段三 | 可信与运维 | ✅ 完成 | Webhook HMAC-SHA256 签名(端到端验签)、失败重试队列、多平台格式(飞书/钉钉/Slack)、日志保留策略 |
| 阶段四 | 感知深化 | ✅ 完成 | 自定义 Sigma 规则在线管理、Syslog 网络监听、攻击场景自动重构、攻击链视图、异常指数、趋势预测 |

## 二、功能清单(按模块)

### 落地页(index.html)
- 开场动画(盾牌描边 / 字母点亮 / 加载计数 / 幕布升起,可跳过)
- 日食主视觉(Canvas 粒子环 + 星空 + 鼠标视差)、威胁跑马灯
- 客户评价轮播、定价月/年切换、FAQ、scrollspy 导航高亮

### 认证与账号
- 注册(密码强度评分)/ 登录(记住邮箱、限速 8 次/10 分钟/IP)/ 登出
- scrypt 加盐散列、HTTP-only 会话、会话只存 Token 哈希
- RBAC:admin / analyst / viewer(团队视图可改角色)
- 设置:资料编辑、改密码、最近登录、JSON 备份导出/导入

### 数据接入(真系统核心)
- 采集器 Token 管理(仅创建时明文显示一次)
- Agent 三种模式:`--file`(tail 真实日志)、`--generator`(压力流)、`--syslog <port>`(UDP 收 syslog)
- Ingest API:批量上报 → SQLite(2 万条环形 + 7 天保留)

### 检测引擎(server/detect.js)
- 内置 13 条规则(正则 10 + 聚合 1 + 事件 2):SSH 暴破聚合、SQL 注入、路径穿越、XSS、WebShell、恶意文件、C2 心跳、DNS 隧道、勒索、横向移动等
- 自定义规则:在线添加/删除正则规则,实时生效
- 机制:同源冷却 10 分钟、匹配输入含 program 字段

### 安全运营控制台(7 视图)
- **总览**:KPI、安全健康分仪表、趋势图(7/14/30 日 + 四级堆叠 + 线性回归预测 7 天)、攻击类型环形图、攻击雷达、事件流(等级筛选)、异常指数(σ 基线偏离)
- **告警中心**:筛选(等级/状态/日期/搜索)、列排序、分页、批量处置、详情抽屉(证据链/备注/复制 JSON/上一条下一条)、注入测试告警、CSV 导出、网络杀伤链七阶段视图、24 小时分布
- **资产管理**:风险评分、详情抽屉(端口/关联告警)、隔离/恢复/扫描
- **处置剧本**:内置 6 条 + 自定义 CRUD、启停、立即执行
- **报表中心**:周对比、合规达标率、风险 Top5、攻击来源 Top5、IP 封禁名单、攻击场景重构、30 天热力图(点击联动)、全球威胁来源分布、周报生成
- **团队**:成员、角色管理、在线状态、名下待处置
- **设置**:资料、密码、通知偏好、订阅规则、Webhook(签名密钥)、数据接入、审计日志(admin)、登录历史

### 通知体系
- 铃铛未读数 + 标签页标题 `(n)` + favicon 红点
- 桌面通知(级别偏好 + 订阅规则引擎)
- Webhook 推送(HMAC 签名、飞书/钉钉/Slack 格式自动适配、失败重试 3 次)

## 三、质量保障

- **20 项 API 集成测试**(`node tests/api.test.js`):认证/CSRF/RBAC/Ingest/检测聚合/溯源/导出/路径屏蔽/审计 — 当前 20/0 全过
- **CI**:GitHub Actions(push/PR 自动跑语法检查 + 全部测试)
- **安全机制**:scrypt 散列、参数化 SQL、CSP、路径屏蔽、登录限速、Webhook 签名
- **部署**:Docker Compose(数据卷 + 健康检查)

## 四、已知限制(诚实清单)

1. **流量数据源**:Agent 目前支持文件 tail / syslog / 压力生成器;尚未内置 NetFlow/PCAP/EDR 级采集
2. **SQLite 单文件**:不支持水平扩展;高并发场景需迁移 PostgreSQL(表结构已可平移)
3. **单进程**:SSE/检测在同进程,大规模部署需拆分管道(消息队列)
4. **无 TLS 终止**:需由反向代理(Nginx/Caddy)承担
5. **Webhook 重试队列在内存**:进程重启后未投递的推送丢失
6. **无多租户**:所有用户同属一个组织
7. **检测规则为简化子集**:未实现完整 Sigma 规范(管道/引用/多条件)

## 五、路线图(R20+)

- [ ] MITRE ATT&CK 战术覆盖矩阵
- [ ] 态势要素雷达图(六维评估)
- [ ] 告警关联图谱(攻击路径图形化)
- [ ] NetFlow / Suricata EVE JSON 接入
- [ ] PostgreSQL 迁移 + 多组织隔离
- [ ] Webhook 持久化队列 + 完整 Sigma 规范
- [ ] 移动端适配打磨(手势已支持)
