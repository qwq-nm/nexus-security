# NEXUS · 使用手册

> 面向:部署者与安全分析师 · 配套版本:v2.0
> 遇到问题先看 [故障排查](#八故障排查)。

## 一、启动系统

### 1. 真实模式(推荐)

```bash
npm install
npm start          # → http://localhost:3000
```

- 数据库:SQLite,位于 `data/nexus.db`(自动创建,WAL 模式)
- 演示账号:`demo@nexus.sec` / `demo1234`(管理员)
- 也可注册新账号:首个账号自动成为管理员,其余为分析师

### 2. Docker 部署

```bash
docker compose up -d    # → http://localhost:3000,数据持久化在 ./data
```

### 3. 演示模式(无后端)

把整个目录托管为静态站点(如 GitHub Pages)即可——前端探测不到后端时自动切换为浏览器演示模式(localStorage 存储数据)。

---

## 二、账号与角色

| 角色 | 权限 |
| --- | --- |
| **admin** 管理员 | 全部权限 + 审计日志 + 成员角色管理 + 备份导入 |
| **analyst** 分析师 | 查看全部 + 告警处置 + 采集器管理 + 剧本管理 + 导出 |
| **viewer** 只读 | 仅查看(无处置/导出/接入操作) |

- 角色由管理员在「团队」视图中调整
- 每次登录、处置、配置变更都会写入审计日志(管理员在设置页查看)

---

## 三、数据接入(让平台感知真实网络)

### 第 1 步:创建采集器

控制台 → **设置 → 数据接入** → 输入名称 →「创建采集器」→ **立即复制 Token(仅显示一次)**。

### 第 2 步:运行 Agent(在同一台或任意能访问平台的机器上)

```bash
# 模式 A:tail 真实日志文件(最常用)
node agent/agent.js --url http://你的平台:3000 --token <TOKEN> --file /var/log/auth.log

# 模式 B:压力生成器(无真实日志时演示全流程)
node agent/agent.js --url http://localhost:3000 --token <TOKEN> --generator

# 模式 C:Syslog 网络监听(接收网络设备/服务器的 UDP syslog)
node agent/agent.js --url http://localhost:3000 --token <TOKEN> --syslog 5514
# 设备端:logger -n <Agent主机IP> -P 5514 -d "测试消息"
```

Agent 每 2 秒批量上报;日志写入 SQLite(7 天保留 + 2 万条环形上限)。

### 第 3 步:看告警

接入的日志自动经过检测引擎(13 条内置规则),命中即生成 `DET-*` 告警并实时推送到控制台(可配置 Webhook / 桌面通知)。

### 自定义检测规则

设置 → **检测规则** → 添加正则规则(名称 + 等级 + 类型 + 模式),立即对新接入日志生效。示例:

| 名称 | 模式 | 等级 |
| --- | --- | --- |
| Java 反序列化 | `serializable\|ObjectInputStream` | 严重 |
| 疑似挖矿连接 | `mining\|stratum\+tcp` | 高危 |

---

## 四、控制台使用

### 快捷键

| 按键 | 作用 |
| --- | --- |
| `1` – `7` | 切换视图(总览/告警/资产/剧本/报表/团队/设置) |
| `Ctrl / Cmd + K` | 命令面板(跳转、搜告警、执行操作) |
| `←` / `→` | 告警抽屉打开时切换上一条/下一条 |
| `Esc` | 关闭抽屉与弹窗 |
| `?` | 快捷键帮助 |

### 总览
- **KPI**:今日告警、已拦截、待处置、资产数、**安全健康分**(综合评分)
- **异常指数**:当日速率相对 14 天基线的 σ 偏离(>2σ 判定异常)
- **趋势图**:7/14/30 日切换;四级堆叠面积(低/中/高/危)+ 总量线 + 拦截虚线 + **7 天预测**(琥珀虚线);悬停看每日分解
- **攻击雷达**:新事件实时变成衰减光点
- **事件流**:可按 全部/异常/正常 筛选

### 告警中心
- 筛选:等级/状态/日期范围/关键词;表头点击排序;分页浏览
- **点击告警行**打开详情抽屉:等级/来源/资产/处置人/证据链/**原始日志摘录**/**命中规则**/**分析师备注**(可写)
- 处置:封禁 / 隔离(确认弹窗)/ 忽略 / 标记解决 / 解除 / 重新处理
- 批量:勾选多行 → 批量标记解决 / 批量忽略
- 注入测试告警(红队演练)、CSV 导出(跟随当前筛选)
- 顶部**攻击链**:告警按杀伤链七阶段分布

### 资产管理
- 点击资产行 → 详情抽屉(端口服务、关联告警)
- 操作:隔离(确认)/ 恢复上线 / 发起扫描(风险分变化)

### 处置剧本
- 内置 6 条;可新建/删除自定义剧本,开关启停
- 「立即执行」会自动处置一条待办告警并写入事件流

### 报表中心
- 本周 vs 上周对比、合规达标率、风险 Top5、攻击来源 Top5
- **IP 封禁名单**(一键全部解封)
- **攻击场景重构**:同源 30 分钟内多次告警自动聚合成攻击链
- **30 天热力图**:点击某天 → 跳转该日告警
- **全球威胁来源分布**:区域节点 + 攻击弧线动画
- **生成本周周报**:下载 Markdown 周报

### 团队(admin)
- 成员卡片:在线状态、加入日期、名下待处置
- 角色下拉直接调整成员角色(不能降级自己)

### 设置
- 资料编辑、修改密码、最近登录
- 通知偏好(桌面通知触发等级)+ **订阅规则**(类型 + 最低级别,命中才通知)
- **Webhook**:填入推送地址自动生成签名密钥;接收端用 `X-NEXUS-Signature` 头(HMAC-SHA256)验真;「测试」按钮可发测试推送
- 数据接入(采集器管理)、JSON 备份导出/**导入**(admin)
- 审计日志(admin)

---

## 五、Webhook 对接示例

推送格式自动适配:URL 含 `open.feishu.cn` / `oapi.dingtalk.com` / `hooks.slack.com` 时按对应 IM 格式发送;其他地址发原始 JSON:

```json
{"source":"NEXUS","level":"crit","msg":"…","ts":1730000000000}
```

验真:每个请求带 `X-NEXUS-Signature: sha256=<hmac>`(对原始请求体做 HMAC-SHA256,密钥见设置页)。失败自动重试 3 次。

---

## 六、API 概览

完整列表见站点 `docs.html`。要点:

```
POST /api/auth/login | register | logout      会话认证
GET  /api/bootstrap                           控制台数据一次拉全
POST /api/ingest/logs                         采集器上报(Bearer Token)
POST /api/alerts/:id/action                   处置告警(7 种动作)
POST /api/alerts/simulate                     注入测试告警
GET  /api/rules | /api/audit | /api/presence  规则/审计/在线状态
GET  /api/stream                              SSE 实时事件流
GET  /api/export/alerts.csv                   CSV 导出(支持筛选参数)
```

---

## 七、部署形态

| 形态 | 说明 |
| --- | --- |
| 本机 `npm start` | 开发/单组织使用,SQLite |
| Docker Compose | 生产推荐:`docker compose up -d` |
| 反向代理 | 建议前置 Nginx/Caddy 做 HTTPS |
| GitHub Pages | 自动进入演示模式(无后端) |

---

## 八、故障排查

| 现象 | 处置 |
| --- | --- |
| 登录报"尝试过于频繁" | 登录限速(10 分钟 8 次/IP),等待或重启服务 |
| 操作报"CSRF 校验失败" | 会话过期,刷新页面重新登录 |
| Agent 报 Token 无效 | Token 被吊销,到设置页重新创建 |
| Agent 一直连接失败 | 检查 `--url` 与平台可达性;平台重启后 Agent 会自动重试 |
| 通知面板收不起来 | 已在 v2.0 修复;若复现请强刷(Ctrl+F5) |
| Webhook 测试失败 | 目标地址 5 秒内不可达;检查接收端与防火墙 |
| 演示模式/真实模式判断 | 前端探测 `api/auth/me`:404=演示模式,200/401=真实模式 |
| 数据清零了? | 检查 `data/nexus.db` 是否被删;备份导入会覆盖当前数据(有确认弹窗) |
